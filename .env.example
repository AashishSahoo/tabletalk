# Environment for this Next.js app: development or production.
# Next.js reserves NODE_ENV, so use this explicit application setting instead.
NODE_ENVIRONMENT=development

# Express API origin only. Do not add /api or an API version here.
API_URL=http://localhost:5000

# URL encoded into restaurant QR codes.
APP_URL=http://localhost:3000
